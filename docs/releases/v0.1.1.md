# v0.1.1 - Operational Ready Checkpoint

Date: 2026-07-02

Status: local-only release checkpoint. The tag has not been pushed unless an operator does it explicitly.

## Highlights

- Compact `mail-agent ops` daily command.
- Read-only diagnostics, review, and handoff flow.
- Schema metadata version 1.
- Gmail transient retries.
- ISO-normalized mail and scheduler timestamps.
- Windows CI workflow.
- Docs and handoff updated for daily operation.

## Safety Boundaries

- `ops`, `health`, and `review` are read-only.
- No Gmail or Telegram live side effects occur unless an explicit `notify-new` or related command is run.
- `.env`, secrets, credentials, and the local SQLite DB are not part of the release.

## Verification

- `pytest -q` => 74 passed.
- `mail_agent ops --skip-scheduler` => ok/low.
- `git diff --check` => OK.

## Tags

- `v0.1.0` points to `04e900e`.
- `v0.1.1` points to `ccf38eb`.
- This release-notes document was added after the tag, at `d3485ae`.
- A tag-at-docs checkpoint should use a new tag, for example `v0.1.2`.
